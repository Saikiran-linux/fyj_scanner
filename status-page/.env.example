# Required. Same values used by the scanner.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_or_eyJ_...

# Required for the /matches page (resume → jobs). Used server-side only by
# app/api/match. Without BOTH, the Matches page returns a 503.
#   VOYAGE_API_KEY — embeds the résumé with voyage-4-large @ 1024d, the same
#                    space as jobs.embedding (must match the scanner's model).
#   OPENAI_API_KEY — JD-precis + gpt-4o-mini rerank chat calls.
VOYAGE_API_KEY=pa-...
OPENAI_API_KEY=sk-proj-...

# The service-role key never reaches the browser — only Server Components
# in app/page.jsx read it. If you'd rather use the anon key, you'll need
# read-only RLS policies on the views first.
